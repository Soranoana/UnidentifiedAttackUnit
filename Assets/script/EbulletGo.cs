using UnityEngine;
using System.Collections;

public class EbulletGo : MonoBehaviour {
	// 弾丸発射点
	//public Transform Emuzzle;
	// 弾丸の速度
//	private float Espeed = 50;
	// Use this for initialization
	void Start () {
		/*transform.GetComponent<Renderer> ().material.color = Color.blue;
		// 弾丸の位置を調整
		transform.position = Emuzzle.position;
		transform.LookAt (Emuzzle.position + transform.forward);
		Vector3 Eforce;
		Eforce = this.gameObject.transform.forward * Espeed;
		// Rigidbodyに力を加えて発射
		transform.GetComponent<Rigidbody> ().AddForce (Eforce, ForceMode.VelocityChange);*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
